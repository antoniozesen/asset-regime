# asset-regime

Cross-asset Streamlit dashboard.

## Run locally

```bash
pip install -r requirements.txt
streamlit run app.py
```

## Streamlit Community Cloud

This repo includes a `requirements.txt` so Streamlit Cloud installs all runtime dependencies (`plotly`, `yfinance`, etc.) before launching `app.py`.
